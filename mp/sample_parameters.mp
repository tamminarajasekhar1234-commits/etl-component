# --------------------------------------------------
# Ab Initio Parameter File (MP)
# POC sample for CI pipeline validation
# --------------------------------------------------

$DB_SCHEMA = "public";
$BATCH_SIZE = 1000;
$ENVIRONMENT = "dev";

# File paths
$INPUT_DIR  = "/data/input";
$OUTPUT_DIR = "/data/output";

# Flags
$ENABLE_AUDIT = true;
